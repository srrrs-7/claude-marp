node_modules/
docs/dist/
*.tsbuildinfo
bun.lock

# PDF source drop — `bun run generate:index` mirrors these into docs/pdf/,
# and only that published copy is tracked (see CLAUDE.md "Standalone PDFs").
/pdf/

# Environment
.env
.env.local

# Agent team workspaces (transient)
.agent-teams/

# Incremental build cache
.cache/

# OS
.DS_Store
Thumbs.db
core
